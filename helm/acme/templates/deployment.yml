apiVersion: apps/v1
kind: Deployment
metadata:
  name: "acme"
spec:
  selector:
    matchLabels:
      app: "acme"
  replicas: {{ .Values.replicaCount }}
  template:
    metadata:
      labels:
        app: "acme"
    spec:
      containers:
      - image: {{ .Values.image }}
        name: "acme"
        env:
          - name: VTT_DBHOST
            value: {{ .Values.dbhost }}
        ports:
        - containerPort: 3000
